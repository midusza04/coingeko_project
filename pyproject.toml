[project]
name = "project"
version = "0.1.0"
description = "Cryptocurrency market data ingestion from CoinGecko API — databases course project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.33.1",
]
